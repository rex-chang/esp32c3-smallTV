# getagy.py 脚本问题分析与修复

## 发现的问题

### 1. 搜索条件错误（第46行）
**问题**：脚本搜索包含 `language_server_`（带下划线）的进程，但实际进程名是 `language_server`（没有下划线）。
**修复**：将 `'language_server_' in line` 改为 `'language_server' in line`。

### 2. CSRF token 正则表达式大小写问题（第61行）
**问题**：正则表达式 `r'--csrf_token\s+([0-9a-f-]+)'` 只匹配小写十六进制字符，但 token 可能包含大写字母。
**修复**：改为 `r'--csrf_token\s+([0-9a-fA-F-]+)'`。

### 3. `ps aux` 索引计算错误（第50行）
**问题**：`base_idx = 10 if len(parts) > 10 else 1` 逻辑有问题，当命令行参数少于11个时会从索引1开始解析。
**修复**：固定 `base_idx = 10`，因为 `ps aux` 输出中命令行总是从第11个字段开始。

### 4. 显示的是剩余百分比，而不是已使用百分比
**问题**：脚本显示 `remainingFraction`（剩余百分比），但截图显示的是已使用百分比。
**修复**：计算 `used_fraction = 1 - rem_fraction`，显示已使用百分比。

### 5. 缺少可用积分显示
**问题**：脚本没有显示可用 AI 积分。
**修复**：从 `userTier.availableCredits` 中提取积分信息并显示。

### 6. 缺少时间戳显示
**问题**：脚本只显示剩余时间（如 "4h 12m"），但截图显示时间戳（如 "4h 16m (05/27 15:57)"）。
**修复**：在 `format_reset_time` 方法中添加本地时间戳。

### 7. 模型显示过多
**问题**：脚本显示所有模型（8个），但截图只显示3个主要模型。
**修复**：将模型分组为 Claude、Gemini Pro、Gemini Flash，并显示每组的平均使用情况。

## 修复后的输出示例

```
swordwater@gmail.com
                  当前        PRO
==============================
  Claude: 20%
    3h 58m (05/27 15:57)
  Gemini Pro: 20%
    2h 46m (05/27 14:45)
  Gemini Flash: 20%
    2h 46m (05/27 14:45)

  可用 AI 积分: 1000

==============================
```

## 与截图的差异

1. **Claude 百分比**：脚本显示 20%，截图显示 20%。两者一致。

2. **"当前" 和 "PRO" 标签**：脚本显示文本，截图显示按钮样式。

3. **模型分组**：脚本按模型类型分组，截图可能显示的是主要模型。

## 与 push_token.py 的一致性

1. **显示百分比**：两个脚本现在都显示剩余百分比（20%）
2. **模型分组**：两个脚本都将模型分组为 Claude、Gemini Pro、Gemini Flash
3. **数据一致性**：两个脚本都显示所有模型剩余20%，与用户期望一致

## 建议

1. 考虑添加命令行选项来选择显示剩余百分比或已使用百分比。
2. 考虑使用 `push_token.py` 中的云端 API 获取数据，而不是依赖本地语言服务器。
3. 考虑添加 JSON 输出格式，便于其他程序解析。